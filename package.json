{
  "name": "better-ghsa",
  "version": "0.0.2",
  "private": true,
  "description": "Browser extension that adds triage state to GitHub security advisories",
  "license": "Apache-2.0",
  "scripts": {
    "check": "tsc --noEmit",
    "package": "npx --yes web-ext@10.6.0 build --artifacts-dir scratch/dist --overwrite-dest",
    "test": "node --test --test-concurrency=1"
  },
  "devDependencies": {
    "@types/node": "^20.19.43",
    "linkedom": "^0.18.5",
    "typescript": "^5.6.3"
  }
}
